// import retrieveUsers from '../helpers/retrieveUsers';
// beforeEach(() => {
//   fetch.resetMocks();
// });


// describe('fetches leaderboard data from the leaderboard API', () => {
//   it("returns a JSON object with a key of 'result'", () => retrieveUsers().then(jsonObject => {
//     expect(
//       Object.prototype.hasOwnProperty.call(jsonObject, 'result'),
//     ).toBeTruthy();
//   }))
// })